      - uses: actions/checkout@v3

      # Make changes to pull request here

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v4- name: Create Pull Request
        with:
          add-paths: sitemap.xml
          delete-branch: true;
